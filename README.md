# AppRover backend

## Run ...

### Common
```shell
poetry shell

poetry install --root-only

poetry run {script_name} 
```

### Run dev

```shell
poetry install --with dev

poetry run dev
```

### Run tests
```shell
poetry run tests
```
